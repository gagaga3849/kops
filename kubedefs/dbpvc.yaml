apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pv-claim
  labels:
    app: vprodb
spec:
  storageClassName: default
  accessModes:
  - ReadWriteOce
  resources:
    requests:
      storage: 3Gi
